# Cross-modal attention pipeline (src/). Python 3.10+
#
# GPU note (RTX 5060 / Blackwell, sm_120): install the CUDA 12.8 build of
# torch instead of the default wheel:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
torch>=2.2
numpy>=1.24
pandas>=2.0

# Legacy TensorFlow baseline (codes/) deps are listed in codes/requirements-legacy.txt
